import { getMetadata } from "../components/Header/MetaData/metadata";


export const metadata = getMetadata(
	"Redux toolkit",
	"Simple state management"
);

const ReduxToolkit = () => {
	return (
		<>
			<div>Redux toolkit page</div>
		</>
	);
};

export default ReduxToolkit;
